/**
 * Next.js configuration. Component: client. Status: complete (default config -- nothing
 * project-specific needed yet).
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
